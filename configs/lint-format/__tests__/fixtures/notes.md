# Test Fixtures

This directory contains fixture data used by the test suite.

## Files

- `config.json` — strict JSON sample
- `config.jsonc` — JSON with comments
- `config.json5` — JSON5 sample
- `styles.css` — CSS sample
- `data.yaml` — YAML sample
